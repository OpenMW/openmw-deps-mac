using darwin : : ${CMAKE_CXX_COMPILER} :
    <compileflags>-mmacosx-version-min=${CMAKE_OSX_DEPLOYMENT_TARGET}
    <compileflags>"-isysroot ${CMAKE_OSX_SYSROOT}"
    <cxxflags>-stdlib=libc++
    <linkflags>-mmacosx-version-min=${CMAKE_OSX_DEPLOYMENT_TARGET}
    <linkflags>-stdlib=libc++
    <linkflags>"-isysroot ${CMAKE_OSX_SYSROOT}"
;
